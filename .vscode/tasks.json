{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run all tests",
            "type": "shell",
            "command": "go test -p 1 ./...",
            "problemMatcher": "$go"
        },
        {
            "label": "Format all go",
            "type": "shell",
            "command": " gofmt -s -w ./"
        }
    ]
}
